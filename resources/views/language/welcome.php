<!DOCTYPE html>
<html lang="{{ app()->getLocale() }}">
<head>
    <meta charset="UTF-8">
    <title>{{ __('messages.welcome') }}</title>
</head>
<body>
    {{ __('messages.welcome') }}
    <button>{{ __('messages.login') }}</button>
</body>
</html>
